setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    api: Dockerfile.api
    web: Dockerfile.web
release:
  image: web
    command:
      - npm run build
run:
  api: npm run start
  web: npm run start